const COM_BASE_Buttons = {
    L1: function () {
        presetSelected = (presetSelected === lastButton ? null : lastButton);
        Load_Page(currentPage);
    },
    L2: function () {
        presetSelected = (presetSelected === lastButton ? null : lastButton);
        Load_Page(currentPage);
    },
    L3: function () {
        presetSelected = (presetSelected === lastButton ? null : lastButton);
        Load_Page(currentPage);
    },
    L4: function () {
        presetSelected = (presetSelected === lastButton ? null : lastButton);
        Load_Page(currentPage);
    },
    L5: function () {
        presetSelected = (presetSelected === lastButton ? null : lastButton);
        Load_Page(currentPage);
    },
    R1: function () {
        presetSelected = (presetSelected === lastButton ? null : lastButton);
        Load_Page(currentPage);
    },
    R2: function () {
        presetSelected = (presetSelected === lastButton ? null : lastButton);
        Load_Page(currentPage);
    },
    R3: function () {
        presetSelected = (presetSelected === lastButton ? null : lastButton);
        Load_Page(currentPage);
    },
    R4: function () {
        presetSelected = (presetSelected === lastButton ? null : lastButton);
        Load_Page(currentPage);
    },
    R5: function () {
        presetSelected = (presetSelected === lastButton ? null : lastButton);
        Load_Page(currentPage);
    }
}

const COM_BASE_Preset = {
    T1: function () {
        freqSelected = "VHF";
        Load_Page(currentPage);
    },
    T2: function () {
        freqSelected = "UHF";
        Load_Page(currentPage);
    },
    T3: function () {
        freqSelected = "FM1";
        Load_Page(currentPage);
    },
    T4: function () {
        freqSelected = "FM2";
        Load_Page(currentPage);
    },
    T5: function () {
        freqSelected = "HF";
        Load_Page(currentPage);
    }
}